import SwiftUI

@main
struct GuitarTabScrollerApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
                .frame(minWidth: 700, minHeight: 800)
        }
        .windowStyle(.hiddenTitleBar)
    }
}
